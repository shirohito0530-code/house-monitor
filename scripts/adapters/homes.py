from .base import PropertyAdapter


class HomesAdapter(PropertyAdapter):

    def __init__(self, config):
        self.config = config

    def search(self, search_config):
        properties = []

        # HOME'S用の取得処理

        return properties
